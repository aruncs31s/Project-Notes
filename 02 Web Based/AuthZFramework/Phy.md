# Philosophy 

The `azf` framework and you existing application must allow on one thing , and the one thing only that , it should share a common medium to access the user information , like to list all users in the current db and assign permission to them.